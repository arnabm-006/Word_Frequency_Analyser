# Word_Frequency_Analyser
This Python Program works by taking a paragraph, converting all letters to lowercase, and removing punctuation so that words like "The," and "the" are treated identically. It then splits the text into individual words, tallies their totals, and prints a list of the words sorted from most frequent to least frequent.
